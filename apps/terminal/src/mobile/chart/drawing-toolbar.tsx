// Copyright (c) 2026 Juan Ignacio Molina Estrada
// SPDX-License-Identifier: FSL-1.1-Apache-2.0
/**
 * Mobile drawing toolbar (design screen 1): five LRU tool slots, grid button
 * (opens the drawing-tools sheet), divider, magnet, trash. Owned by WS-D —
 * replace this file's contents; the default export is the contract. Rendered
 * through MobileChartSurface's `footer` prop, docked above the tab bar,
 * only while the Chart tab is bare.
 *
 * Stand-in: renders nothing so the chart floor ships without tools until
 * WS-D lands.
 */
export default function MobileDrawingToolbar() {
  return null
}
