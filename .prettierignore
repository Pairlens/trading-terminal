package-lock.json
pnpm-lock.yaml
yarn.lock
.agents/
documents/

# Auto-generated (GitNexus regenerates these; don't fight the generator)
AGENTS.md
CLAUDE.md
.claude/skills/gitnexus/

# Build artifacts checked into the repo
# NOTE: a workspace with its own `format` script (today: apps/terminal) does
# not see this file — Prettier reads the .prettierignore in its CWD. Entries
# below that live inside such a workspace are mirrored in its own
# .prettierignore, and a test keeps the two in step.
apps/terminal/public/_sdk/

# Generated / gitignored build artifacts (regenerated by their tooling)
apps/desktop/src-tauri/gen/
apps/terminal/public/_pyodide/
ds-bundle/
packages/ui/.ds-tmp/

# Build output, anywhere. Nothing under a dist/ is tracked, so formatting it
# is 500+ pointless writes per run into bundles the dev server may be serving.
dist/

# Local design-handoff reference material (untracked scratch, not for the repo)
design_handoff_marketing_site/

# Design handoff bundles — reference material, not code
design_handoff*/
